<?xml version="1.0" encoding="utf-8" ?>
<pages:PopupPage
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    CloseWhenBackgroundIsClicked="True"
    xmlns:pages="clr-namespace:Rg.Plugins.Popup.Pages;assembly=Rg.Plugins.Popup"
    Title="{Binding Title}"
    x:Class="UniFindr_V2.Views.UniversityDetailsPage"
    x:Name="UniversityDetails">

    <!--You can use any elements here which are extended from Xamarin.Forms.View-->
    <StackLayout 
        VerticalOptions="Center" 
        HorizontalOptions="Center">
        <Label x:Name="UniName"
               Style="{DynamicResource Title}"/>
        <Label x:Name="UniCountry"
               Style="{DynamicResource Subtitle}"/>
        <Label x:Name="UniArea"/>
        <Button Text="Visit Website"
                Clicked="OpenUniversityWebsite"/>
        <Button Text="Favourite"
                Clicked="AddToFavourites"/>
        <Button Text="Back"
                Clicked="ReturnToList"/>
    </StackLayout>
</pages:PopupPage>